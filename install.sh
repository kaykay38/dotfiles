#!/bin/bash

ln -s $(pwd)/.* "$HOME/"
# ln -s ./.config/* "$HOME/.config/"
# ln -s ./.config/* $HOME/.config/
# for file in $(pwd)/.*
# do
#     ln -s "$file" "$HOME/$file" echo "Linked $file"
# done
# 
# for dir in ./.config/
# do
#     ln -s "$dir" "$HOME/.config/$dir" echo "Linked $dir"
# done
